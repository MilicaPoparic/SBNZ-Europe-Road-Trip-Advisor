export interface SearchParams {
    localFood?: string[],
    transportation?: string[],
    budget?: string,
    children?: Boolean,
    numberOfPeople?: Number,
    accommodation?: Number,
    startDate?: Date,
    endDate?: Date,
    maxDistance?: Number
    
    
}
